
/*
// Animação de cor no hover do nome
const span = document.getElementById('logo-span');
const colors = ['var(--first-color)', 'var(--second-color)', 'var(--third-color)'];
let hoverCount = 0;

span.addEventListener('mouseenter', () => {
    const nextColor = colors[hoverCount % colors.length];
    span.style.color = nextColor;
    hoverCount++;
});

span.addEventListener('mouseleave', () => {
    span.style.color = 'var(--main-color)';
});
*/


